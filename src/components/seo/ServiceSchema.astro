---
import { SITE, PRICING, TELEGRAM } from '@config/site';

const schema = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  name: SITE.name,
  description: 'Hajj and Umrah Badal (proxy pilgrimage) on behalf of any deceased Muslim — parents, relatives, friends, or martyrs — performed from within Makkah under full shariah supervision.',
  provider: {
    '@type': 'Organization',
    name: SITE.name,
    url: SITE.url,
  },
  areaServed: ['US', 'CA', 'SE', 'NO'],
  offers: [
    {
      '@type': 'Offer',
      name: 'Hajj Badal',
      price: PRICING.hajjBadal,
      priceCurrency: PRICING.currency,
    },
    {
      '@type': 'Offer',
      name: 'Umrah Badal',
      price: PRICING.umrahBadal,
      priceCurrency: PRICING.currency,
    },
  ],
  serviceType: 'Proxy Hajj / Umrah',
  availableChannel: {
    '@type': 'ServiceChannel',
    serviceUrl: TELEGRAM.url,
  },
};
---

<script type="application/ld+json" set:html={JSON.stringify(schema)}></script>
